import Quickshell
import qs.components.bar
import qs.components.systemmenu
import qs.components.controlpanel

ShellRoot {
    Bar {
        id: bar
    }
    SystemMenu {
        id: systemmenu
    }
    ControlPanel {
        id: controlpanel
    }
}
